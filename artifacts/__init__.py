"""The artifact model: what a thing is, what produces it, and how a request
becomes an ordered list of work.

A package rather than loose modules so it can be imported from the project
root -- which is also what puts config.py within reach, for the commit
stamped into every manifest.
"""
